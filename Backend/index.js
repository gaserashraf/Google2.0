/*const mysql = require("mysql");
const express = require("express");
const bodyParser = require("body-parser");
//const myConnection = require('./server');

const app = express();
app.use(bodyParser.json());

app.listen(5000, () => {
    console.log('Server initiated succesfully');
});
app.get("/", (req, res) => {
    res.send("Im");
})

app.use((req, res) => {
    res.render("404");
})*/